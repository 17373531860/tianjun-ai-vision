# language: zh-CN
功能: USB 键盘扫码枪 (即插即用, v3.20)
  作为用扫码枪查工单 / 绑工件的产线
  我希望把 USB 扫码枪当成扫码器设备的一种来管理
  以便插上键盘式扫码枪就能用, 无需配置网络地址

  背景:
    假设 后端处于测试模式

  场景: 创建一把 USB 扫码枪设备应落库且类型正确
    当 我创建一把用途为绑工件的 USB 扫码枪
    那么 扫码枪响应状态应为 200
    并且 该设备应能在扫码器列表查到
    并且 该设备类型应为 "usb_hid"

  场景: 两把 USB 扫码枪可以共存不撞地址
    当 我创建一把用途为拉工单的 USB 扫码枪
    并且 我再创建一把用途为绑工件的 USB 扫码枪
    那么 两次创建都应成功

  场景: USB 扫码枪不带 IP 端口也能创建 (不参与地址唯一校验)
    当 我创建一把不带网络地址的 USB 扫码枪
    那么 扫码枪响应状态应为 200

  场景: 模拟扫码注入应能进入绑工件链路而不崩
    当 我模拟扫一个工件码
    那么 扫码注入响应状态应为 200

  场景: 删除 USB 扫码枪设备应成功
    假设 已有一把 USB 扫码枪
    当 我删除该 USB 扫码枪
    那么 扫码枪响应状态应为 200
    并且 该设备不应在扫码器列表出现
