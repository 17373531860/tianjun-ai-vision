# language: zh-CN
功能: 外部MES按规格自动切换检测项目
  作为 产线MES对接
  我希望 虚拟MES推来工单产品码/规格时系统自动切到对应检测项目
  以便 换产免人工切项目, 且规格命名千变万化(前缀变/后缀变/中间固定/无分隔符)都能命中

  背景:
    假设 重置入站对接配置

  场景: 对照表精确命中即切项目
    假设 已有检测项目 "PEXACT"
    并且 入站开启切项目 按名匹配关 严格边界关 映射 {"PEXACT": "PEXACT"}
    当 虚拟MES推送产品码 "PEXACT"
    那么 当前激活项目应为 "PEXACT"

  场景: 通配符吃掉天天变的后缀
    假设 已有检测项目 "PSUFA"
    并且 入站开启切项目 按名匹配关 严格边界关 映射 {"PSUFA-*": "PSUFA"}
    当 虚拟MES推送产品码 "PSUFA-250628"
    那么 当前激活项目应为 "PSUFA"

  场景: 通配符吃掉变化的前缀
    假设 已有检测项目 "PPREB"
    并且 入站开启切项目 按名匹配关 严格边界关 映射 {"*-PPREB": "PPREB"}
    当 虚拟MES推送产品码 "20260628-PPREB"
    那么 当前激活项目应为 "PPREB"

  场景: 通配符吃掉两头变中间固定
    假设 已有检测项目 "PMIDC"
    并且 入站开启切项目 按名匹配关 严格边界关 映射 {"*PMIDC*": "PMIDC"}
    当 虚拟MES推送产品码 "L7-PMIDC-R9"
    那么 当前激活项目应为 "PMIDC"

  场景: 自动同名子串兜底-项目名是产品码的一段
    假设 已有检测项目 "NSUBA"
    并且 入站开启切项目 按名匹配开 严格边界关 映射 {}
    当 虚拟MES推送产品码 "NSUBA-77"
    那么 当前激活项目应为 "NSUBA"

  场景: 自动同名子串兜底-无分隔符也命中
    假设 已有检测项目 "NNOSEP"
    并且 入站开启切项目 按名匹配开 严格边界关 映射 {}
    当 虚拟MES推送产品码 "NNOSEP001"
    那么 当前激活项目应为 "NNOSEP"

  场景: 多个同名子串取最长项目名不误吞短名
    假设 已有检测项目 "NL"
    并且 已有检测项目 "NLONG"
    并且 入站开启切项目 按名匹配开 严格边界关 映射 {}
    当 虚拟MES推送产品码 "NLONG-1"
    那么 当前激活项目应为 "NLONG"

  场景: 按名匹配默认关时不切项目-存量客户零影响
    假设 已有检测项目 "DOFF"
    并且 入站开启切项目 按名匹配关 严格边界关 映射 {}
    当 虚拟MES推送产品码 "DOFF-1"
    那么 项目 "DOFF" 不应被激活

  场景: 严格边界拦住短名被错误吞掉
    假设 已有检测项目 "SBX"
    并且 入站开启切项目 按名匹配开 严格边界开 映射 {}
    当 虚拟MES推送产品码 "SBXMORE-1"
    那么 项目 "SBX" 不应被激活

  场景: 严格边界下贴分隔符仍正常命中
    假设 已有检测项目 "SBSEP"
    并且 入站开启切项目 按名匹配开 严格边界开 映射 {}
    当 虚拟MES推送产品码 "SBSEP-1"
    那么 当前激活项目应为 "SBSEP"
