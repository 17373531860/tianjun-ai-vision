# language: zh-CN
功能: 齐件即结算 + 扫码器生命周期闭环 (捷昌二期)
  作为装配线操作员
  我希望所有物品放齐并确认后周期立刻结算 (不用等物品离开)
  并且扫码器按"码-合格-码"节奏工作:
  OK 才重新亮灯、亮灯时作废旧码、已合格的码永久拒绝
  以便扫一个码只对应一个合格件, 不串码不漏码

  背景:
    假设 后端处于测试模式

  # ==========================================================================
  # A: 齐件即结算 — 项目配置面 (开关 + 步骤级确认帧数落库/读回)
  # ==========================================================================

  场景: 项目开启齐件即结算并配置确认放入帧数后能原样读回
    当 我创建一个 ROI离开 跟踪项目并开启齐件即结算, 螺丝步骤确认帧数设为 5
    那么 读回项目 pipeline_config.tracking_settle_on_complete 应为 True
    并且 读回项目螺丝步骤的 settle_confirm_frames 应为 5

  场景: 不开齐件即结算的项目读回时该开关不为真 (现状零差异)
    当 我创建一个 ROI离开 跟踪项目且不带齐件即结算配置
    那么 读回项目 pipeline_config.tracking_settle_on_complete 不应为 True

  # ==========================================================================
  # B: 扫码器生命周期 — 设备配置面 (三个新字段落库/默认值)
  # ==========================================================================

  场景: 新建扫码器不带生命周期字段时默认值等于现状行为
    当 我 POST 一台最小配置的 LON 扫码器
    那么 读回设备 resume_on 应为 "cycle_end"
    并且 读回设备 rearm_forget_last 应为 False
    并且 读回设备 strict_ok_dedup 应为 False

  场景: 新建扫码器带 仅合格亮灯+作废旧码+强制去重 配置后能原样读回
    当 我 POST 一台 resume_on=ok_only 且开作废旧码和强制去重的 LON 扫码器
    那么 读回设备 resume_on 应为 "ok_only"
    并且 读回设备 rearm_forget_last 应为 True
    并且 读回设备 strict_ok_dedup 应为 True

  # ==========================================================================
  # B: 扫码器生命周期 — 运行时行为 (NG 灭灯阻塞 → 人工恢复出口)
  # ==========================================================================

  场景: 仅合格模式下 NG 结算后扫码器保持灭灯阻塞
    假设 工位 0 挂着一台 resume_on=ok_only 且等待周期恢复的扫码器连接
    当 周期以 NG 结算触发扫码器恢复
    那么 该扫码器应处于恢复阻塞状态
    并且 工位 0 的 is_resume_blocked 应为 True

  场景: 人工恢复端点是 NG 阻塞的出口
    假设 工位 0 挂着一台 resume_on=ok_only 且等待周期恢复的扫码器连接
    当 周期以 NG 结算触发扫码器恢复
    并且 我 POST /api/v1/scanner/resume?channel_id=0
    那么 响应状态应为 200
    并且 恢复响应 resumed 列表应包含该扫码器
    并且 工位 0 的 is_resume_blocked 应为 False

  场景: 默认周期结束模式下 NG 结算也照常恢复亮灯 (现状零差异)
    假设 工位 0 挂着一台 resume_on=cycle_end 且等待周期恢复的扫码器连接
    当 周期以 NG 结算触发扫码器恢复
    那么 该扫码器不应处于恢复阻塞状态

  场景: 没有等待恢复的扫码器时人工恢复端点优雅返回
    当 我 POST /api/v1/scanner/resume?channel_id=0
    那么 响应状态应为 200
    并且 恢复响应 resumed 列表应为空
