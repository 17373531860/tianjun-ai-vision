# language: zh-CN
功能: 区域事件模式组合矩阵
  作为工位流程监测项目的质量工程师
  我希望模型只检测"物"(工具/工件/手)时
  系统能用时序+空间规则(重叠持续N帧/进区后消失)产出动作事件
  并映射进周期/步骤/计数器体系
  以便不重训模型就能监测 测硬度→扫码→下工件 的工位流程

  背景:
    假设 通道 0 检测器与虚拟剧本源处于停止状态

  场景: 顺序模式项目带区域事件配置时零差异直通
    假设 一个顺序模式项目, 但配置里残留了区域事件规则
    当 注入工具持续重叠工件的剧本并启动检测
    那么 原始标签"测硬度笔"应作为普通步骤计数
    并且 不应出现事件步骤"测硬度"

  场景: 区域事件模式但规则为空时退回普通步骤统计
    假设 一个区域事件模式项目, 但未配置任何规则
    当 注入工具持续重叠工件的剧本并启动检测
    那么 原始标签"测硬度笔"应作为普通步骤计数
    并且 不应出现事件步骤"测硬度"

  场景: 重叠规则连续满帧确认事件
    假设 一个标准三规则的区域事件项目
    当 注入完整工位循环剧本并启动检测
    那么 事件步骤计数应包含 "测硬度,扫码,下工件"
    并且 周期应结算为合格
    并且 results 载荷规则清单应为 "测硬度,扫码,下工件"

  场景: 工具短暂划过不足最少帧数不产事件
    假设 一个标准三规则的区域事件项目
    当 注入工具只划过一瞬的剧本并启动检测
    那么 不应出现事件步骤"测硬度"
    并且 合格计数应保持不变

  场景: 漏检中断在容忍范围内仍确认事件
    假设 一个中断容忍很大的区域事件项目
    当 注入重叠中途反复漏检的剧本并启动检测
    那么 事件步骤计数应包含 "测硬度"

  场景: 从未进过下料区的常驻工件消失不触发结算
    假设 一个标准三规则的区域事件项目
    当 注入台面常驻工件直接消失的剧本并启动检测
    那么 合格计数应保持不变

  场景: 每类置信度低于门限的检测不参与判定
    假设 一个测硬度笔置信度门限较高的区域事件项目
    当 注入低置信度工具重叠工件的剧本并启动检测
    那么 不应出现事件步骤"测硬度"

  场景: 顺序校验开启时乱序结算记乱序计数
    假设 一个开启顺序校验的区域事件项目
    当 注入先扫码后测硬度的乱序剧本并启动检测
    那么 乱序计数应至少增加 1
    并且 周期应结算为合格

  场景: 顺序校验关闭时乱序零差异不记数
    假设 一个关闭顺序校验的区域事件项目
    当 注入先扫码后测硬度的乱序剧本并启动检测
    那么 周期应结算为合格
    并且 乱序计数应保持不变

  场景: 结算判定按标准序列命中合格
    假设 一个配置了结算判定的区域事件项目
    当 注入完整工位循环剧本并启动检测
    那么 周期应结算为合格

  场景: 结算判定缺测硬度按自定义规则判不良
    假设 一个配置了结算判定的区域事件项目
    当 注入缺测硬度直接扫码下料的剧本并启动检测
    那么 周期应结算为不合格

  场景: 结算判定重复扫码按自定义规则判不良
    假设 一个配置了结算判定的区域事件项目
    当 注入扫码两次的完整循环剧本并启动检测
    那么 周期应结算为不合格

  场景: 进区规则对象入区满帧确认事件
    假设 一个含进区规则的区域事件项目
    当 注入工具持续重叠工件的剧本并启动检测
    那么 事件步骤计数应包含 "上料"
