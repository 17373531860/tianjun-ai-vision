# language: zh-CN
功能: 萍乡百斯特两阶段流水线称重 (drive_mode=pipeline) 全功能链路
  作为 现场质量工程师
  我希望 工件坐秤装料 + 上一件压机侧收尾的两阶段产线被软件全自动驱动
  以便 秤指令(去皮/清零)自动发出、重量事实离秤冻结、收尾动作结案后直写达梦中间表

  背景:
    假设 通道已登记流水线称重模式
    并且 已选择产品型号 "型号A"

  场景: 标准两阶段周期 — 上件自动去皮进入装料相位
    当 秤上放上自重 1.2 公斤的工件并保持稳定
    那么 系统发出去皮指令
    并且 状态机进入 "filling" 相位
    并且 当前皮重记为 1.2 公斤

  场景: 装料合格离秤 — 重量事实冻结并进入待收尾队列
    假设 工件已上秤去皮完成
    当 装料到净重 3.0 公斤并保持稳定
    并且 工件被拿离秤台并确认离秤
    那么 离秤结算判定为 "ok"
    并且 待收尾队列有 1 件
    并且 系统调度清零指令

  场景: 收尾标签结案 — 加钢脚水泥确认后队头件按已确认结案
    假设 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列
    当 视觉连续识别到加钢脚水泥动作
    那么 队头件按 "label" 状态结案
    并且 待收尾队列有 0 件
    并且 称重记录已落库且判定为 "ok"

  场景: 缺料件离秤 — 判定不合格并触发缺料报警
    假设 工件已上秤去皮完成
    当 装料到净重 2.5 公斤并保持稳定
    并且 工件被拿离秤台并确认离秤
    那么 离秤结算判定为 "shortage"
    并且 事件流中出现 "shortage" 报警

  场景: 皮重超范围 — 放错物品报警且不去皮
    当 秤上放上自重 8.0 公斤的工件并保持稳定
    那么 事件流中出现 "tare_range" 报警
    并且 系统未发出去皮指令
    并且 状态机保持 "empty" 相位

  场景: 收尾超时兜底 — 超时未见收尾动作按未确认结案并报警
    假设 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列
    当 收尾超时时间已过且秤面持续空秤
    那么 队头件按 "timeout" 状态结案
    并且 事件流中出现 "finalize_timeout" 报警
    并且 待收尾队列有 0 件

  场景: 双件并行 — 第二件称重时第一件仍在待收尾队列且先进先出结案
    假设 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列
    当 第二件自重 1.3 公斤上秤去皮并装料到 3.05 公斤后离秤
    那么 待收尾队列有 2 件
    并且 队列顺序为先进先出
    当 视觉连续识别到加钢脚水泥动作
    那么 结案的是第一件
    并且 待收尾队列有 1 件

  场景: 快手兜底 — 清零未发出新件已上秤时皮重按差值折算
    假设 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列且清零延迟很长
    当 清零指令发出前新件自重 1.4 公斤直接上秤并保持稳定
    那么 系统发出去皮指令
    并且 当前皮重记为 1.4 公斤
    并且 状态机进入 "filling" 相位

  场景: 视觉守卫限区 — 钢脚水泥盆区域出现加水泥动作触发报警
    假设 视觉守卫配置了加水泥动作仅允许在钢帽料盆区域
    当 视觉连续在钢脚料盆区域识别到加水泥动作
    那么 事件流中出现 "guard_restrict" 报警

  场景: 视觉守卫料别映射 — 大盆区域舀料动作映射为钢帽水泥
    假设 视觉守卫配置了大盆区域舀料映射钢帽水泥料别
    当 视觉连续在大盆区域识别到舀料动作
    那么 状态机当前视觉料别为 "钢帽水泥"

  场景: 结案直写虚拟达梦 — 收尾确认后中间表新增一行且字段对齐
    假设 已配置数据库直写连接指向虚拟达梦中间表
    并且 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列
    当 视觉连续识别到加钢脚水泥动作
    那么 虚拟达梦中间表新增 1 行
    并且 该行净重为 3.0 且判定为 "ok" 且收尾状态为 "confirmed"

  场景: 超时结案也直写虚拟达梦 — 收尾状态为未确认
    假设 已配置数据库直写连接指向虚拟达梦中间表
    并且 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列
    当 收尾超时时间已过且秤面持续空秤
    那么 虚拟达梦中间表新增 1 行
    并且 该行净重为 3.0 且判定为 "ok" 且收尾状态为 "unconfirmed"

  场景: API 面 — 虚拟喂重量接口推进状态机且看板状态可查
    当 通过接口喂入一帧 1.2 公斤读数
    那么 看板状态接口返回该通道实时重量 1.2 公斤
    并且 看板状态接口返回 "pipeline" 驱动模式

  场景: API 面 — 称重记录接口返回已落库记录
    假设 一件净重 3.0 公斤的工件已离秤结算进入待收尾队列
    那么 称重记录接口返回至少 1 条记录
    并且 最新记录净重为 3.0 判定为 "ok"
