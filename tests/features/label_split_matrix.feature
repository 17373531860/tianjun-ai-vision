# language: zh-CN
功能: 同标签区域拆分组合矩阵
  作为装配线质量工程师
  我希望模型只认识"打螺丝"一个动作时
  系统能按打的位置(固定画面或跟随工件)、按工序轮次(前罩/后罩)
  把动作拆成对角顺序的虚拟步骤, 打错顺序当场报警
  以便客户不用重训模型就能验对角打螺丝工艺

  背景:
    假设 通道 0 检测器与虚拟剧本源处于停止状态

  场景: 未配置拆分规则时原始标签直通 (零差异)
    假设 一个只认"打螺丝"标签且未配置拆分规则的项目
    当 注入持续打螺丝的剧本并启动检测
    那么 原始标签"打螺丝"应作为普通步骤计数
    并且 不应出现虚拟步骤"螺丝1"

  场景: 固定画面四象限拆分, 对角打完判合格
    假设 一个四象限拆分项目, 区域定位方式为"固定画面"
    当 注入按对角顺序打完四颗螺丝的剧本并启动检测
    那么 步骤计数应包含 "螺丝1,螺丝2,螺丝3,螺丝4"
    并且 周期应结算为合格

  场景: 锚点跟随模式下工件整体偏移仍正确拆分
    假设 一个四象限拆分项目, 区域定位方式为"锚点跟随"
    当 注入工件整体偏移后打完四颗螺丝的剧本并启动检测
    那么 步骤计数应包含 "螺丝1,螺丝2,螺丝3,螺丝4"
    并且 周期应结算为合格

  场景: 多轮次拆分, 前后罩两轮共八个虚拟步骤
    假设 一个四象限拆分项目, 区域定位方式为"固定画面"
    并且 该项目启用多轮次拆分, 盖罩切换共 2 轮
    当 注入两轮各打四颗螺丝的剧本并启动检测
    那么 步骤计数应包含 "前罩螺丝1,前罩螺丝4,后罩螺丝1,后罩螺丝4"
    并且 周期应结算为合格

  场景: 每轮独立区域, 翻面后位置偏移仍正确命中
    假设 一个单区域拆分项目并启用多轮次, 第 2 轮使用整体右移的独立区域
    当 注入第二轮在右移位置打螺丝的剧本并启动检测
    那么 步骤计数应包含 "后罩螺丝1"
    并且 第二轮打在老位置的螺丝不应被计入任何步骤

  场景: 配置违序即时事件后, 跳打螺丝当场报警
    假设 一个严格顺序四象限拆分项目且已配置违序即时事件
    当 注入直接越序打第二颗螺丝的剧本并启动检测
    那么 违序计数器应至少增加 1
    并且 越序的"螺丝2"不应计步

  场景: 未配置违序即时事件时守门只拦不报 (零差异)
    假设 一个严格顺序四象限拆分项目但未配置违序即时事件
    当 注入直接越序打第二颗螺丝的剧本并启动检测
    那么 违序计数器应保持不变
    并且 越序的"螺丝2"不应计步
