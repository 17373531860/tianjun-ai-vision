# language: zh-CN
功能: 传感器清洁 工位1 同帧双类别计数门槛
  作为传感器清洁工序的产线工程师
  我希望配置"计数伴随标签"后, 只有画面同帧检出锚动作与伴随动作两个类别时才计一件产品
  以便对齐现场 detect9 判定, 消除单类别误检导致的多计

  背景:
    假设 传感器清洁插件已加载并挂上帧级检测钩子

  场景: 未配置伴随标签时仅锚标签即可计数 (v1.2.0 零差异)
    假设 插件配置未启用计数伴随标签
    当 主程序逐帧广播"仅锚动作出现并移动"的检测序列
    那么 看板总产量应为 1

  场景: 启用伴随标签后仅锚标签不计数
    假设 插件配置的计数伴随标签为"清洁产品"
    当 主程序逐帧广播"仅锚动作出现并移动"的检测序列
    那么 看板总产量应为 0
    并且 不应触发任何主程序事件

  场景: 启用伴随标签后同帧双类别正常计数
    假设 插件配置的计数伴随标签为"清洁产品"
    当 主程序逐帧广播"锚动作与伴随动作同帧出现并移动"的检测序列
    那么 看板总产量应为 1
    并且 应触发一次合格计件事件

  场景: 跟踪中途伴随标签消失按锚框离场处理
    假设 插件配置的计数伴随标签为"清洁产品"
    当 主程序逐帧广播"第二帧起伴随动作消失"的检测序列
    那么 看板总产量应为 0

  场景: 伴随标签保存热加载后新门槛立即生效
    假设 插件配置未启用计数伴随标签
    当 通过配置接口保存计数伴随标签为"清洁产品"并热加载
    并且 主程序逐帧广播"仅锚动作出现并移动"的检测序列
    那么 重新读取的配置伴随标签应为"清洁产品"
    并且 看板总产量应为 0
