# language: zh-CN
功能: 传感器清洁 工位1 双类别计数许可与按标签 ROI
  作为传感器清洁工序的产线工程师
  我希望配置"计数许可标签"后, 只有它出现过(不必与锚动作同帧)才允许锚动作移动计件,
  并且每个判定标签可以框定 ROI 区域, 区域外的检出一律不算数
  以便对齐现场 detect9(1) 判定: 消除只动单类别导致的误计, 且屏蔽画面无关区域的干扰

  背景:
    假设 传感器清洁插件已加载并挂上帧级检测钩子

  场景: 未配许可标签时仅锚标签即可计数 (v1.2.0 零差异)
    假设 插件配置未启用计数许可标签
    当 主程序逐帧广播"仅锚动作出现并移动"的检测序列
    那么 看板总产量应为 1

  场景: 启用许可标签后只移动锚标签不计数 (客户反馈场景)
    假设 插件配置的计数许可标签为"脏污产品"
    当 主程序逐帧广播"仅锚动作出现并移动"的检测序列
    那么 看板总产量应为 0
    并且 不应触发任何主程序事件

  场景: 两类交替出现按 detect9(1) 许可解锁计数
    假设 插件配置的计数许可标签为"脏污产品"
    当 主程序逐帧广播"先出现许可动作、随后锚动作单独出现并移动"的检测序列
    那么 看板总产量应为 1
    并且 应触发一次合格计件事件

  场景: 计到一件后许可被消费需再次解锁
    假设 插件配置的计数许可标签为"脏污产品"
    当 主程序逐帧广播"许可解锁计一件后锚动作再次单独移动"的检测序列
    那么 看板总产量应为 1

  场景: 锚标签配置 ROI 后区域外移动不计数
    假设 插件配置未启用计数许可标签
    并且 插件配置锚标签的 ROI 为画面左半区
    当 主程序逐帧广播"锚动作在画面右半区出现并移动"的检测序列
    那么 看板总产量应为 0

  场景: 锚标签配置 ROI 后区域内移动正常计数
    假设 插件配置未启用计数许可标签
    并且 插件配置锚标签的 ROI 为画面左半区
    当 主程序逐帧广播"锚动作在画面左半区出现并移动"的检测序列
    那么 看板总产量应为 1

  场景: 许可标签保存热加载后新语义立即生效
    假设 插件配置未启用计数许可标签
    当 通过配置接口保存计数许可标签为"脏污产品"并热加载
    并且 主程序逐帧广播"仅锚动作出现并移动"的检测序列
    那么 重新读取的配置许可标签应为"脏污产品"
    并且 看板总产量应为 0
