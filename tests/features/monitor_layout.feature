# language: zh-CN
功能: 检测主页自定义布局的保存与恢复 (v3.54)
  作为产线组长
  我希望把检测主页各区块的排版按页面形态分别保存到系统数据库
  软件升级和重启后不丢，改乱了也能一键恢复出厂排版

  场景: 保存单工位自定义布局后能读回
    当 我保存形态 "single:default" 的布局且视频区块位于 x=0.10 y=0.05
    那么 布局响应状态应为 200
    并且 读取全部布局应包含形态 "single:default" 且视频区块 x=0.10

  场景: 越界坐标被钳制到画布内而不是拒收
    当 我保存形态 "single:default" 的布局且视频区块位于 x=1.50 y=-0.20
    那么 布局响应状态应为 200
    并且 读回形态 "single:default" 的视频区块坐标应全部落在 0 到 1 之间

  场景: 缺少版本号的布局被拒收不落库
    当 我保存形态 "dual" 的无版本号布局
    那么 布局响应状态应为 400
    并且 读取全部布局应不包含形态 "dual"

  场景: 非法形态键被拒收
    当 我用非法形态键 "single:BAD!" 保存布局
    那么 布局响应状态应为 400

  场景: 拖没了的区块最小尺寸兜底
    当 我保存形态 "triple" 的布局且视频区块宽高均为 0
    那么 布局响应状态应为 200
    并且 读回形态 "triple" 的视频区块宽高应不小于 0.01

  场景: 三工位 sop 槽位可被自定义布局保存读回
    当 我保存形态 "triple" 的布局且区块 "sop" 位于 x=0.02 y=0.02
    那么 布局响应状态应为 200
    并且 读取全部布局应包含形态 "triple" 且区块 "sop" x=0.02

  场景: 恢复单个形态默认不影响其他形态
    假如 形态 "single:default" 与 "triple" 均已保存自定义布局
    当 我删除形态 "triple" 的布局
    那么 布局响应状态应为 200
    并且 读取全部布局应包含形态 "single:default" 且不包含形态 "triple"

  场景: 全部恢复默认清空所有形态
    假如 形态 "single:default" 与 "triple" 均已保存自定义布局
    当 我删除全部自定义布局
    那么 布局响应状态应为 200
    并且 读取全部布局应为空

  场景: 删除不存在的形态是幂等操作
    当 我删除形态 "zoom" 的布局
    那么 布局响应状态应为 200
