# language: zh-CN
功能: 外部 MES 工单主动拉取 (v3.20)
  作为对接外部 MES 的产线
  我希望能主动去外部 MES 查询工单并回填到本地
  以便不依赖外部系统推送也能拿到工单

  背景:
    假设 后端处于测试模式

  场景: 拉取连接列表端点可用
    当 我 GET 拉取连接列表
    那么 拉取响应状态应为 200

  场景: 外部 MES 连不上时测试拉取应优雅返回失败而不崩
    当 我用一个连不上的地址测试拉取
    那么 拉取响应状态应为 200
    并且 测试结果 success 应为 False

  场景: 上银正常返回时测试拉取应识别出数组路径与字段
    假设 外部 MES 将返回 1 条上银工单
    当 我用上银模板测试拉取
    那么 测试结果 success 应为 True
    并且 识别出的数组路径应为 "response.resultData"

  场景: 立即同步应把工单落库
    假设 外部 MES 将返回 1 条上银工单
    并且 已建一条上银拉取连接
    当 我对该连接执行立即同步
    那么 拉取结果 created 应为 1
    并且 该工单应能在工单列表查到

  场景: 试同步不应把工单落库
    假设 外部 MES 将返回 1 条上银工单
    并且 已建一条上银拉取连接
    当 我对该连接执行试同步
    那么 拉取结果 created 应为 1
    并且 该工单不应在工单列表出现

  场景: 上银返回错误时应带回 errorInfo 详情
    假设 外部 MES 将返回 500 错误带 errorInfo
    并且 已建一条上银拉取连接
    当 我对该连接执行立即同步
    那么 拉取结果 success 应为 False
    并且 拉取错误信息应包含 "SYSTEM ERROR"
