# language: zh-CN
功能: MES 工单接收 - 最新开工为准(顶替边界)
  作为对接生产管控系统的推理端
  我希望同一产品同一工位只保留最新一条开工任务
  以便上游重发开工时直接顶替当前进行中的任务, 并把被顶替的任务回传完工

  # 客户亲定边界(原话): 同一产品同一工位只会有一个任务;
  # 以最新开工信号为准直接顶替当前进行的任务; 发来开工信息必须反馈完工信息。

  场景: 新开工顶替进行中的旧任务
    假设 入站已配置开工建单且最新开工为准
    当 上游先后发来开工任务 "BDD-A" 和 "BDD-B"
    那么 工单 "BDD-A" 状态应为 completed
    并且 工单 "BDD-B" 状态应为 in_progress

  场景: 不开启顶替时两条任务并存
    假设 入站已配置开工建单但不顶替
    当 上游先后发来开工任务 "BDD-C" 和 "BDD-D"
    那么 工单 "BDD-C" 状态应为 in_progress
    并且 工单 "BDD-D" 状态应为 in_progress

  场景: 顶替时必须向上游回传完工
    假设 入站已配置开工建单顶替并回传完工
    当 上游先后发来开工任务 "BDD-E" 和 "BDD-F"
    那么 工单 "BDD-E" 状态应为 completed
    并且 上游应收到 "BDD-E" 的完工回传
