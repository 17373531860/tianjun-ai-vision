# 生产环境配置
#
# ⚠ 故意不设 VITE_API_BASE_URL —— 它会在 vite build 时被**字面替换**进 dist，
#   一旦写死成 http://localhost:8001，一体机浏览器打开工作站页面后会把所有请求
#   打给一体机自己（那台机器上没有后端）→ 整页 Network Error。
#
# 留空后由 src/api/backendTarget.js 按运行形态解析：
#   Electron 出厂壳 (file://) → http://localhost:8001    （与以前完全一致）
#   一体机浏览器 (http://<工作站IP>:8001) → 同源相对路径  （一拖多靠这条）
#
# 需要把前端指向另一台机器的后端时，用 .env.production.local 覆盖（不进仓库）。
VITE_WS_URL=/ws
