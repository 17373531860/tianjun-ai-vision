# 开发环境配置
#
# 同样故意不设 VITE_API_BASE_URL：写死 localhost 后，从别的机器开
# http://<开发机IP>:6001 调试时，API 会被打到访问者自己的 localhost。
# 留空由 src/api/backendTarget.js 按页面 hostname 拼开发后端端口 (8001)。
#
# 副本 worktree 后端跑 8002 等非默认端口时，在 .env.development.local 里写
#   VITE_API_BASE_URL=http://127.0.0.1:8002/api/v1
# 覆盖（.local 优先级更高且不进仓库，见 start-dev-servers skill）。
VITE_WS_URL=/ws
