# 上银包装线 AI 视觉检测 — 工单拉取接口对接问讯（致贵司 IT）

> 日期：2026-06-23
> 事由：现场设备已能成功调用贵司工单查询接口（HTTP 200），但返回的工单数据始终为空，
> 需贵司协助确认测试区数据 / 鉴权 / 是否改用正式环境。

---

## 一、现状（已排除的问题）

我方设备已严格按贵司提供的接口文档完成配置，并验证如下，**问题不在网络与请求格式**：

1. **网络已通**：设备能访问 `https://itweb.hiwin.cn/java_demo_test/api`，每次调用都正常返回，
   响应 `statusCode = 200`、`success = true`。
2. **请求格式正确**：按贵司文档使用 `HTTP POST` + `application/json;charset=UTF-8`，
   请求体结构、`api` 字段、`parameters.job_no` 均与文档一致（请求样例见第五节）。
3. **解析路径正确**：按文档取 `response.resultData` 数组、按 `statusCode=200` 判成功。

## 二、现存问题

**无论用哪个工单号查询，`response.resultData` 始终返回空数组 `[]`（拉回 0 条）。**

包括贵司文档"API 访问正确时"示例中出现的工单号 **`JOB150300021-3`**（文档示例返回客户名
"厦门聚锐机电科技有限公司"、排产量 500、规格 EGW20CA ZAC），我方实测查询该单返回：

- `statusCode = 200`、`success = true`
- 但 `response.resultData = []`（空）

即：**接口正常应答，但查不到任何工单数据。**

## 三、需贵司确认 / 提供（测试区）

1. **测试区 `java_demo_test` 当前是否有可查询的工单数据？** 是否已被清空或停用？
2. 请提供 **3～5 个当前测试区中真实存在、可查询到数据的工单号（`job_no`）**，供我方联调验证。
3. 调用测试区是否需要**鉴权**（Token / API-Key / 账号密码）？若需要，请提供测试用凭据及放置位置
   （请求头字段名 / 请求体字段）。
4. 测试区是否对**来源 IP 做白名单**限制？若是，请告知需放行的设备公网出口 IP
   （我方可提供现场设备出口 IP）。

## 四、需贵司确认 / 提供（正式 / 内网环境）

若测试区不再维护、或贵司希望直接对接正式环境，请提供：

1. **正式环境工单查询接口的完整地址与路径**（现场内网地址）。
2. 正式环境的**鉴权方式与凭据**（Token / API-Key / 账号密码）。
3. 现场设备访问正式接口是否需要**内网 IP 白名单 / 防火墙放行**？需放行的地址我方可提供。
4. 提供 **3～5 个正式环境中真实存在的工单号**，供现场联调。

> 说明：我方设备部署在贵司现场工控机、运行于贵司内网，由设备本机直接访问贵司 MES，
> **我方（供应商）不会、也无需远程连入贵司内网。**

## 五、附：我方当前发出的请求（便于贵司比对）

- 请求地址：`https://itweb.hiwin.cn/java_demo_test/api`
- 请求方法：`POST`，`Content-Type: application/json;charset=UTF-8`
- 请求体（以查询 `JOB150300021-3` 为例）：

```json
{
  "api": "hiwin/webcn/ai_error_prevention_job_info/query",
  "parameters": {
    "job_no": "JOB150300021-3"
  }
}
```

- 我方按文档解析：成功判定字段 `statusCode`（期望 200）、工单数组路径 `response.resultData`、
  取字段 `job_no`（工单号）/ `cust_name`（客户名）/ `dispatch_qty`（排产量）/ `spec`（规格）。

---

### 期望贵司反馈清单（小结）

- [ ] 测试区是否有数据 / 是否停用
- [ ] 测试区可用工单号 3～5 个
- [ ] 测试区是否需鉴权（含凭据）
- [ ] 测试区是否需 IP 白名单
- [ ] （如改正式区）正式接口地址 + 路径
- [ ] （如改正式区）正式鉴权方式 + 凭据
- [ ] （如改正式区）是否需内网白名单 + 可用工单号
