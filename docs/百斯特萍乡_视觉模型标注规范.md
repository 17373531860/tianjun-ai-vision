# 萍乡百斯特项目 · 视觉模型标注规范 v2.0

> 适用范围：胶装水泥称重、行为监测及预警项目的 YOLO 检测模型训练。
> 阅读对象：标注人员 + 训练负责人。
> 更新时间：2026-07-13
>
> **v2.0 重大修订**：按现场确认的真实工艺（秤上定量称料，而非工件上秤）重定义标签集与称重联动挂点；吸收 7.12 首版数据集的三条教训（见附录）。**首版模型作废，新模型按本规范从零标注训练。**

---

## 一、先看懂工艺（标注前必读）

现场确认的单件作业流程，秤参与两次、全部发生在秤台上：

```
① 空容器放上秤 ──→ 软件发清零指令，秤归零          【秤：去皮】
② 从料盆舀料/备料                                    【视觉：料源防错】
③ 向秤上容器投料，工人看读数装到目标重量            【秤：重量判定】
④ 把称好的料端到压机，倒入压机中的工件              【视觉：收尾】
```

**秤称的是"料"，不是工件**。重量判定发生在第③步（装料到位当场判合格/缺料/超量），不是第④步。第④步倒进工件后秤上已经没有东西可判——这是 v1.0 的根本性错误，v2.0 已纠正。

## 二、总原则

1. **标的是"动作"，不是"料"**。两种水泥外观无法可靠区分，料别由「动作发生在哪个料盆区域」+ 当班选定的型号在软件里判定（区域在项目配置里画，不进模型）。所以标签集里**没有**任何"钢帽/钢脚"字样——**标签名带料名是 7.12 版的缺陷之一**，那会导致每种料都要养一个标签。
2. **标"进行中"，不标"静置"**：所有标签都要求**手（或料流）在画面里参与**。物体静静摆着的帧一律不标——7.12 版把"容器静置在秤上"标成了动作，模型学成了"看见容器就报"，无人时也常亮，此教训必须吸取。
3. **动作框 = 最小完整包络**：框住完成该动作所需的全部要素（手 + 工具/器具 + 接触对象），不带无关背景。
4. **宁缺毋滥**：模糊帧、遮挡过半帧直接丢弃不标。防错场景**误报比漏报更伤信任**，脏样本是误报的第一来源。

## 三、标签集（4 个）

标签名在项目里全部可改，以下为建议默认名。

| # | 标签名 | 定义（何时算这个动作） | 框的范围 | 称重联动 |
|---|---|---|---|---|
| 1 | `上秤清零` | 手接触容器且容器位于秤台（放置/扶正过程） | 手 + 容器 + 秤台局部 | **去皮门控**：软件发清零指令，秤确认归零后本步放行 |
| 2 | `舀料` | 手持工具**接触料面**开始，到工具**离开料面**结束 | 手 + 工具 + 接触的料面局部 | 无；**料源防错主判定动作**（框落在哪个盆区=取的哪种料） |
| 3 | `装杯` | 料流向秤上容器（持杯倒、整盆倾倒均算），到器具回正 | 器具 + 手 + 秤上容器口 | **重量判定门控**：读数爬到目标并稳定→比标准量；缺料/超量当场报警拦截 |
| 4 | `压机加料` | 称好的料在压机处倒入工件，杯口倾斜到回正 | 杯 + 手 + 压机工件口 | 无秤联动；末步收尾（可选：秤读数回落确认料已端走） |

> 若现场最终工序与上表不一致，以现场确认的 SOP 为准增删标签，规则不变。

## 四、正确 / 错误示例（现场真实帧，逐标签过）

> 截图取自现场三段视频（新机位作业视频为主）。**绿色实线框 = 正确标注**，**红色虚线框 = 错误标注**。标注前先把本节所有图过一遍，对齐口径再动手。

### 4.1 `上秤清零`：框 = 手 + 秤上容器 + 秤台局部

**✔ 正确**——手在容器上，动作进行中：

![上秤清零正确示例A](百斯特萍乡_标注示例图/fig_zero_ok_A.jpg)

**✔ 正确**——另一工人另一机位，要件相同（手接触 + 容器在秤上）：

![上秤清零正确示例B](百斯特萍乡_标注示例图/fig_zero_ok_B.jpg)

**✘ 错误：静置帧打标**——容器只是摆在秤上、无人接触。**这种帧是负样本，绝对不标**。7.12 版大量此类标注直接导致模型"无人也报"：

![上秤清零错误示例-静置](百斯特萍乡_标注示例图/fig_zero_bad_static.jpg)

### 4.2 `舀料`：框 = 手 + 工具 + 接触的料面局部

**✔ 正确**——现机位，三要素齐全：

![舀料正确示例](百斯特萍乡_标注示例图/fig_scoop_ok.jpg)

**✔ 正确**——另一工人另一姿态，标签相同（判定只看要素接触，见第五节）：

![舀料正确示例-另一姿态](百斯特萍乡_标注示例图/fig_scoop_ok2.jpg)

**✘ 错误一：框太大**——整个人、机台、料箱全框进去，背景被学进特征，误报第一来源：

![舀料错误示例-框太大](百斯特萍乡_标注示例图/fig_scoop_bad_big.jpg)

**✘ 错误二：框太小**——只框了手，缺工具和接触料面，模型学不到"在舀什么"：

![舀料错误示例-框太小](百斯特萍乡_标注示例图/fig_scoop_bad_small.jpg)

### 4.3 `装杯`：框 = 器具 + 手 + 秤上容器口

**✔ 正确**——整盆倾倒也是`装杯`（判定要件是"料流向秤上容器"，不限器具）：

![装杯正确示例A](百斯特萍乡_标注示例图/fig_cup_ok_A.jpg)

**✔ 正确**——另一工人持杯倒料，器具不同、要件相同：

![装杯正确示例B](百斯特萍乡_标注示例图/fig_cup_ok_B.jpg)

**✘ 错误：框整个人**——动作标签框的是"动作要素"，不是操作者本人：

![框整个人错误示例](百斯特萍乡_标注示例图/fig_person_bad.jpg)

### 4.4 `压机加料`：框 = 杯 + 手 + 压机工件口

**✔ 正确**——称好的料倒进压机中的工件：

![压机加料正确示例](百斯特萍乡_标注示例图/fig_press_ok.jpg)

**✘ 错误：只框机器**——杯和手都不在框内，学到的是"压机部件"不是"加料动作"：

![压机加料错误示例](百斯特萍乡_标注示例图/fig_press_bad.jpg)

### 4.5 同帧多动作：各标各的，旁观人员不标

**✔ 正确**——双人同帧，两处动作分别框；旁观人员**不打任何框**：

![同帧多动作正确示例](百斯特萍乡_标注示例图/fig_multi_ok.jpg)

### 4.6 负样本：无动作帧留白，不打任何标签

**✔ 正确**——转身/走动/整理的过渡帧、物体静置帧**整帧不标**（留白即负样本）。7.12 版负样本为零是误报的主因之一，**本次负样本必须占数据集 10-15%**：

![负样本示例](百斯特萍乡_标注示例图/fig_negative.jpg)

## 五、工人个体差异怎么处理（判定要件法）

同一个动作，不同工人做出来样子差别很大：有人俯身双手、有人侧身单手，持杯还是整盆倾倒、左右利手、站位朝向都不一样。**如果按"长什么样"标注，模型只会认拍视频那个人**。解决办法是把每个标签定义成一张**判定要件表**——只看"哪些要素发生了接触"，不看人、不看姿态：

| 标签 | 判定要件（全部满足才标） | 明确不看 |
|---|---|---|
| `上秤清零` | ① 容器在秤台上 ② 手接触容器 | 谁放的、从哪个方向放 |
| `舀料` | ① 手持工具 ② 工具接触料面 | 单手/双手、俯身/侧身、左右手 |
| `装杯` | ① 秤上容器在画面 ② 料流向该容器（器具倾斜） | 用杯还是整盆倒、器具颜色 |
| `压机加料` | ① 位置在压机工件口 ② 器具倾斜出料或手在工件口作业 | 站位朝向、持杯手势 |

下图是同一标签、不同工人/器具的实拍对比——**四张图全部是正确标注**：

![不同工人同一动作对比](百斯特萍乡_标注示例图/fig_worker_variance.jpg)

配套的执行手段（标注和训练两侧同时做）：

1. **每人先标定标 50 帧**：每位工人的素材开标前，先由复核人按要件表校准 50 帧作为该工人的"参考答案"，标注员对齐后再量产。
2. **每标签每工人配额**：每个标签在**每位在岗工人**身上 ≥ 300 框；只有一个人的素材再多也不算够（这也是第八节补录清单要求"每位工人都出镜"的原因）。
3. **验证集必须含"没见过的工人"**：留出至少一名工人的素材完全不进训练集，只进验证集。这名工人身上指标掉得多，说明模型在背"人"而不是学"动作"，需要回头补多样性。
4. **拿不准就过要件表**：边界帧犹豫时，对照上表逐条打勾——全满足就标，有一条不满足就丢帧。禁止"看起来像"式标注。
5. **新工人上岗**：补录该工人素材 1 天，按要件表标注后做增量训练；要件表本身不需要改。

## 六、逐条标注规则

1. **时间边界**：动作类标签按"接触开始 → 分离结束"取帧；边界前后 2-3 帧的过渡姿态**不标**。
2. **静置不标**（v2.0 新增硬规则）：容器在秤上没人碰、称好的料摆着没人端——全部留白。判据：**画面里没有"手参与"就不标**。
3. **遮挡**：目标被遮挡超过 50% 的帧丢弃；轻微遮挡（<50%）正常标。
4. **运动模糊**：肉眼看不清工具/器具轮廓的帧丢弃。
5. **同帧多动作**：双人作业各标各的，互不影响；旁观人员不标。
6. **负样本留白**：以下帧**不打任何标签**（留白就是负样本，**占比 10-15%**）：
   - 物体静置：容器在秤上无人碰、空秤台、满/空料盆静置
   - 空手/空勺从盆上方划过
   - 擦手、整理工装、拿放无关物品、转身走动
7. **一致性**：同一动作全程由同一人标完；换标注员前先对照已标样本校准 20 帧。

## 七、抽帧策略

| 素材段 | 抽帧率 | 说明 |
|---|---|---|
| 动作发生段 | 3-5 fps | 动作全程密集采，覆盖起始/中段/收尾姿态 |
| 静置/等待段 | 0.2-0.5 fps | **专门作为负样本采**，凑够全集 10-15% |

## 八、客户补录清单（发给百斯特）

- **机位固定不动**（区域防错依赖固定机位，机位挪了区域要重画）
- 不同**班次**、不同**工人**、不同**光照**（白天/晚上/开关灯）各覆盖 ≥ 2 天
- **每位会上这个工位的工人都要出镜**，每人正常作业 ≥ 30 分钟（支撑第五节"每标签每工人 ≥ 300 框"配额；只录一个熟练工，模型换人就失效）
- 必录场景：
  1. 正常完整作业 ≥ 2 小时
  2. 故意错误示范各 ≥ 10 次：从错误料盆取料、跳过清零直接装料、缺料/超量停手
  3. 双人同时作业（帮工场景）≥ 30 分钟
  4. 静置空镜（无人、容器摆秤上、满盆静置）≥ 20 分钟（负样本原料）
- 分辨率 ≥ 1080p，帧率 ≥ 15fps，避免逆光

## 九、数据集组织

1. **切分防泄漏**：按**时间段**切 train/val（如前 4 天训练、后 1 天验证），禁止随机按帧切（相邻帧几乎一样，随机切=作弊）。
2. **按工人二次切分**：在时间切分之上，留至少一名工人的素材只进验证集（见第五节第 3 条），专测跨工人泛化。
3. **量级目标**：每标签 ≥ 1000 框起步；`舀料`是防错主判定动作，优先堆到 ≥ 2000 框。
4. **类平衡**：某标签样本 < 其他标签 1/3 时，专门补录该动作。
5. **负样本硬指标**：空标签帧占全集 **10-15%**，其中至少一半是"物体静置"类（治"无人也报"）。

## 十、模型验收口径

| 指标 | 门槛 |
|---|---|
| mAP@0.5（验证集） | ≥ 0.85 |
| 现场连跑半天误报 | ≤ 1 次/小时 |
| **静置空镜连放 30 分钟** | **零误报**（针对 7.12 版缺陷的专项验收） |
| 四步 SOP 逐步识别（现场实测 20 件） | 漏步识别 0 次 |
| 跨工人抽测（未进训练集的工人实测 10 件） | 漏步识别 ≤ 1 次 |

未达标优先补数据（按第八节清单定向补录），其次调置信度阈值（在项目"步骤设置"里按标签单独调），最后才考虑换模型规格。

---

## 附录：7.12 首版数据集复盘（为什么作废重来）

首版 2613 帧、四标签（称重清零/舀料/装杯/加钢脚水泥），模型置信度本身不差，但有三个结构性缺陷，参数补救不了根，故作废：

1. **负样本为零**：2613 帧每帧恰好一框，模型没见过"没有动作"的画面 → 静置容器常亮误报、动作飘移到无关区域。
2. **静置当动作标**：「称重清零」把容器静置在秤上（无人接触）也标了 → 标签学成了"状态"，整周期反复闪现，无法当步骤锚点用。
3. **料名进标签**：「加钢脚水泥」把料别写死在标签里 → 换料就要换标签重训，且与"料别由区域判定"的防错设计冲突。

v2.0 规范的"静置不标"硬规则、负样本 10-15% 硬指标、中性标签名，分别对应堵死这三条。
