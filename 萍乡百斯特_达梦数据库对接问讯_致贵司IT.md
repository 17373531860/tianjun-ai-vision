# 达梦数据库对接方案及信息确认函

| | |
|---|---|
| **项目名称** | 胶装水泥称重、行为监测及预警系统（萍乡百斯特） |
| **对接事项** | 产线检测与称重数据回传至贵司达梦数据库 |
| **发起方** | 天军科技 · AI 视觉检测系统 |
| **日期** | 2026 年 7 月 14 日 |

尊敬的贵司 IT 负责人：

为落实检测数据回传事宜，现将对接方案、数据明细及需贵司确认的事项整理如下。本函已包含对接所需的全部技术信息，烦请贵司填写第五节的确认栏后返回，双方即可约定时间联调。

---

## 一、对接方式

我方部署在产线工控机上的检测软件，在每件产品称重判定完成后，直接向贵司达梦数据库中约定的一张中间表写入记录（参数化 INSERT，同一事务提交，失败自动重试并留有通讯日志）。

贵司无需开发任何接口程序，只需完成三件事：**建一张中间表、开一个仅有该表写入权限的账号、防火墙放行工控机到数据库的端口**。

| 项目 | 说明 |
|---|---|
| 连接方式 | 达梦官方 Python 驱动（dmPython）直连数据库 |
| 写入语句 | 参数化 INSERT，无字符串拼接，列名经白名单校验 |
| 事务 | 同一件产品的多行记录在同一事务内提交，失败整体回滚 |
| 异常处理 | 网络抖动或数据库瞬时不可用时自动重试，次数与间隔可配置 |
| 数据兜底 | 我方本地数据库长期留存完整检测原始记录；回传通道故障期间数据不丢失，恢复后可补录 |
| 安全边界 | 仅对约定中间表执行 INSERT，不查询、不修改、不删除任何其他数据库对象 |
| 版本支持 | 达梦 DM8（我方已在 DM8 V8 环境完成全链路写入验证）；如贵司为 DM7 请在确认栏注明 |

---

## 二、回传数据明细

**写入时机与口径**：一件产品（一个工件）完成全部料别的称重判定后写入，每道料一行。例如某型号需投两种料，则该件产品产生 2 行记录。

我方可提供的数据项如下。贵司中间表需要哪几列、列名如何命名均可自定，我方在软件端做字段映射，后续增减字段不涉及代码改动：

| 数据项 | 类型建议 | 说明 | 示例 |
|---|---|---|---|
| 工位号 | VARCHAR(32) | 产线工位编号 | 1 |
| 产品序号 | VARCHAR(64) | 工件序号；有扫码时为条码，无码时为流水号 | 20260714-0001 |
| 产品型号 | VARCHAR(64) | 当前生产型号 | GJ-300 |
| 料别名称 | VARCHAR(64) | 本行记录对应的料种 | 钢帽水泥 |
| 作业员 | VARCHAR(64) | 当班操作人员 | 李四 |
| 皮重 | DECIMAL(10,4) | 去皮时刻的工件/容器自重，单位 kg | 0.5120 |
| 净重 | DECIMAL(10,4) | 实际投料量（去皮归零后的终值），单位 kg | 2.4980 |
| 标准重量 | DECIMAL(10,4) | 该型号该料别的标准投料量，单位 kg | 2.5000 |
| 判定结果 | VARCHAR(16) | 取值见下表 | ok |
| 班次 | VARCHAR(32) | 当前班次名称 | 白班 |
| 完成时间 | TIMESTAMP | 建议由数据库默认值自动生成（见第三节建表语句） | 2026-07-14 12:59:00 |

**判定结果取值**（如贵司系统另有代码规范，请在确认栏提供对照关系，我方按贵司口径映射）：

| 取值 | 含义 |
|---|---|
| ok | 合格，净重在标准量容差范围内 |
| shortage | 缺料，低于标准量下限 |
| over | 超量，高于标准量上限 |

**写入样例**（联调环境实际写入的数据）：

```
工位  料别      皮重     净重     标准量   判定       班次
1     钢帽水泥  0.5120   2.4980   2.5000   ok         白班
2     钢脚水泥  0.5000   1.2010   1.2000   ok         白班
2     钢脚水泥  0.4980   0.9500   1.2000   shortage   白班
```

**数据量预估**：按产线节拍，每工位每分钟约 1～2 行，两工位每日约 1000～3000 行，单行不足 1 KB，对数据库负载影响可忽略。

---

## 三、参考建表语句（DM8）

中间表结构以贵司规范为准。若贵司希望由我方提供参考，可直接使用以下语句（列名、类型、字段增减均可调整，不影响对接）：

```sql
CREATE TABLE T_WEIGH_RECORD (
    ID           BIGINT IDENTITY(1,1) PRIMARY KEY,       -- 自增主键
    STATION      VARCHAR(32),                            -- 工位号
    PRODUCT_SN   VARCHAR(64),                            -- 产品序号
    MODEL_NAME   VARCHAR(64),                            -- 产品型号
    MATERIAL     VARCHAR(64),                            -- 料别名称
    OPERATOR     VARCHAR(64),                            -- 作业员
    TARE_WEIGHT  DECIMAL(10,4),                          -- 皮重(kg)
    NET_WEIGHT   DECIMAL(10,4),                          -- 净重/实际投料量(kg)
    STD_WEIGHT   DECIMAL(10,4),                          -- 标准重量(kg)
    VERDICT      VARCHAR(16),                            -- 判定: ok/shortage/over
    SHIFT_NAME   VARCHAR(32),                            -- 班次
    CREATE_TIME  TIMESTAMP DEFAULT CURRENT_TIMESTAMP     -- 写入时间(库端自动生成)
);

CREATE INDEX IDX_WEIGH_TIME ON T_WEIGH_RECORD(CREATE_TIME);
CREATE INDEX IDX_WEIGH_SN   ON T_WEIGH_RECORD(PRODUCT_SN);
```

建议为本次对接开设最小权限的专用账号：

```sql
CREATE USER TJ_WRITER IDENTIFIED BY "口令由贵司设定";
GRANT INSERT ON T_WEIGH_RECORD TO TJ_WRITER;
```

表内历史数据的清理与归档由贵司自行管理，我方仅执行写入。

---

## 四、网络要求

| 项目 | 内容 |
|---|---|
| 连接方向 | 产线工控机 → 贵司达梦数据库服务器 |
| 端口 | 达梦监听端口（默认 5236，以贵司实际配置为准） |
| 防火墙 | 需为工控机 IP 开放上述端口；工控机 IP 在现场部署时确定，亦可提前按网段放行 |
| 带宽占用 | 每次写入不足 1 KB，可忽略 |

---

## 五、请贵司确认的事项

### （一）数据库环境

| 序号 | 事项 | 贵司确认 |
|---|---|---|
| 1 | 达梦版本（DM7 / DM8，具体版本号） | |
| 2 | 数据库服务器 IP 与端口 | |
| 3 | 实例字符集（UTF-8 / GB18030） | |
| 4 | 防火墙白名单的办理方与流程 | |

### （二）账号与权限

| 序号 | 事项 | 贵司确认 |
|---|---|---|
| 5 | 提供给我方的账号及密码（仅需中间表 INSERT 权限） | |
| 6 | 中间表所在的库 / 模式（schema）名称 | |

### （三）中间表

| 序号 | 事项 | 贵司确认 |
|---|---|---|
| 7 | 表结构由哪方定义；贵司定义请附建表语句，我方定义请确认第三节参考结构 | |
| 8 | 数据口径：每件产品每道料一行，是否符合预期；如需按班次或批次汇总请说明 | |
| 9 | 判定结果沿用 ok / shortage / over，还是按贵司代码规范另行映射 | |
| 10 | 完成时间由库端默认值生成（推荐），还是我方写入；我方写入请注明格式 | |

### （四）联调安排

| 序号 | 事项 | 贵司确认 |
|---|---|---|
| 11 | 联调流程：贵司先建测试表（结构同正式表），我方试写核对后再切正式表，是否可行 | |
| 12 | 贵司 IT 对接人及联系方式 | |
| 13 | 期望联调时间 | |

---

## 六、联调流程

上述信息确认后，双方按以下步骤联调：

1. **连通验证**（约 10 分钟）：我方在工控机软件界面填入连接信息，向测试表试写数据，双方核对字段内容与字符编码；
2. **口径核对**（约半天）：产线试运行，按真实节拍写入，贵司核对行数、数值、判定结果与现场实际是否一致；
3. **切换正式表**：确认无误后，软件界面将目标表切换为正式表，开始正式回传；
4. **异常演练**（可选）：现场模拟断网，验证网络恢复后的重试机制与日志记录，双方确认异常处置口径。

---

## 七、我方承诺

1. 仅对约定中间表执行 INSERT 操作，不查询、不修改、不删除贵司数据库的任何其他对象；
2. 每次写入均在我方本地留有通讯日志（成功/失败/耗时/错误原因），可供追溯；
3. 我方本地数据库长期留存完整检测原始记录，回传通道故障期间数据不丢失，可事后补录；
4. 数据库连接信息在我方软件内加密存储，不向任何第三方传播。

---

## 附：我方已完成的准备工作

- 软件已内置达梦官方驱动，随安装包发货，现场无需额外安装；
- 已在达梦 DM8 V8 真实数据库完成全链路验证，包括建表、单行写入、多行同事务写入、中文字符、DECIMAL 精度、非法列名拦截及回读核对；
- 写入通道复用我方成熟的数据推送框架（同时支持 REST、表单、Modbus、数据库直写等协议），已在多个客户现场稳定运行。

如对本函内容有任何疑问，欢迎随时与我方联系。盼复，谢谢！
