# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.coverage
htmlcov/

# Skill / MCP / agent 工具产物
.mcp.json
skills-lock.json
.agents/

# Virtual environments
venv/
ENV/
env/
.venv/
python-env/

# Database
*.db
*.sqlite3
*.db-shm
*.db-wal

# Uploads and recordings (user data)
backend/uploads/
backend/recordings/
!backend/uploads/.gitkeep
!backend/recordings/.gitkeep

# v3.8.x 定时导出运行时产物 (用户数据, 不入版本库)
backend/exports/

# v3.13+ 插件安装副本 (上传 .tjvplugin 后落地的运行时副本, 不入版本库)
backend/plugins/

# 调参运行时配置 (检测框 Kalman 平滑, 客户机现场调出来的, 不入版本库)
backend/kalman_config.json

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
frontend/dist/
electron/dist/
*.tar.gz

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Environment files
.env
.env.local
.env.*.local

# Temporary files
tmp/
temp/
*.tmp

# Model weights & test data
*.pt
*.avi
*.mp4

# Patch / dist archives
patch*/
patch*.zip
frontend/dist.zip

# Old version folder
tianjun/

# Old/temp test folders (但保留正规 tests/ 目录)
/test/
testdata/
test_output/
test_videos/

# Doc files
*.docx

# License keys and files (NEVER commit)
keys/
licenses/
video_frames/
HCNetSDK*/
*.xlsx
hotfix*/
*.zip
*.lic

# Local debug & probe scripts (not part of product)
probe_*.py
decode_pcap.py
analyze_pcap*.py
*.pcapng
WMax_IDManager_V*/
releases/
tune_rod_conf/

# Ad-hoc test scripts at repo root (should live in tests/ if keep)
/test_cluster.py
/test_external_device.py
/test_flash_scan.py
/test_multi_workstation.py
/test_ondemand.py
/test_parse_config*.py
/test_scanner_*.py
/test_toast_alarm.py
/test_video_*.py

# Runtime state that changes on every run (not source of truth for the product)
backend/counters/*.json
!backend/counters/.gitkeep
backend/data/counters/*.json
!backend/data/counters/.gitkeep
backend/alarm_config.json
backend/current_operator.json
backend/current_user.json
backend/scanner_runtime_state.json
backend/workstation_config.json
backend/device_config.json

# Local ad-hoc scripts at repo root (not product code)
/_bench_camera.py
/_p6_extract.py
/smoke_report.txt

# Internal / reverse-engineering scratch (not for release)
docs/wmax_decompiled/

# Manual auditing intermediate output (subagent reports / scan dumps)
.tmp_audit/

# Stray/duplicate scratch directory
test (2)/

# Field diagnostic & simulation scripts (not part of product)
tools/diag_*.py
tools/test_scan_pair_*.py

# HTML intermediate artifact for manual (PDF is the released form)
docs/*.html

# External skill vendor trees (`npx skills add`, symlink targets)
/.agents/

# Local prototypes & SDK scratch (not for release / kept locally for fast iteration)
prototype/
se9/
wheels_se9/

# 插件签名 dev 密钥 (RSA 私钥 + PLUGIN_SECRET) — 严禁推到 git
dev_keys/
*.pem
PLUGIN_SECRET.txt
!docs/**/*.pem.example
!tests/**/fixtures/**/*.pem

# License 生成工具 (独立工具, 含 license 签名私钥 + 503MB 打包产物 — 不随主仓发布)
tools/license-tool/
